---
import Layout from "../layouts/Layout.astro";
import Navigation from "../components/Navigation.astro";

const features = [
  {
    title: "AI Foundations",
    description:
      "Master core AI concepts, LLMs, and practical developer use cases",
    icon: "🧠",
  },
  {
    title: "Hands-on Learning",
    description: "Build real AI applications using JavaScript and modern tools",
    icon: "💻",
  },
  {
    title: "Expert Guidance",
    description: "Learn industry best practices and future-proof methodologies",
    icon: "🎯",
  },
];

const sessions = [
  {
    number: "01",
    title: "AI Foundations",
    description:
      "Build foundational knowledge about AI concepts and integrate them into your workflow",
    topics: [
      "AI & LLM basics",
      "Practical use cases",
      "JavaScript for AI",
      "Hands-on API practice",
    ],
    href: "/curriculum/01-ai-foundations",
  },
  {
    number: "02",
    title: "Prompt Engineering",
    description:
      "Master the art of crafting effective prompts for different scenarios",
    topics: [
      "Prompt anatomy",
      "Advanced techniques",
      "Chain-of-thought",
      "Real-world examples",
    ],
    href: "/curriculum/02-prompt-engineering",
  },
  {
    number: "03",
    title: "AI-Powered Tools",
    description: "Create practical AI tools using LLMs and APIs",
    topics: [
      "Application architecture",
      "API integration",
      "Error handling",
      "Building a chatbot",
    ],
    href: "/curriculum/03-ai-powered-tools",
  },
  {
    number: "04",
    title: "RAG Systems",
    description:
      "Learn Retrieval-Augmented Generation for knowledge-based AI apps",
    topics: [
      "Vector databases",
      "Embeddings",
      "Similarity search",
      "Knowledge bases",
    ],
    href: "/curriculum/04-rag-systems",
  },
  {
    number: "05",
    title: "AI Agents",
    description: "Build autonomous AI agents with reasoning capabilities",
    topics: [
      "Agent architecture",
      "LangChain",
      "Tool integration",
      "Autonomous systems",
    ],
    href: "/curriculum/05-ai-agents",
  },
  {
    number: "06",
    title: "Architecture",
    description:
      "Design scalable AI systems and integrate them with existing apps",
    topics: [
      "System design",
      "Integration patterns",
      "Ethics & privacy",
      "Best practices",
    ],
    href: "/curriculum/06-architecture",
  },
  {
    number: "07",
    title: "Deployment",
    description: "Deploy, monitor, and optimize your AI applications",
    topics: [
      "Cloud deployment",
      "Performance",
      "Monitoring",
      "Cost optimization",
    ],
    href: "/curriculum/07-deployment",
  },
];

const benefits = [
  {
    title: "Practical Skills",
    description: "Build real-world AI applications from day one",
    icon: "🛠️",
  },
  {
    title: "Career Growth",
    description: "Position yourself at the forefront of AI development",
    icon: "📈",
  },
  {
    title: "Expert Support",
    description: "Get guidance from experienced AI practitioners",
    icon: "👥",
  },
  {
    title: "Community",
    description: "Join a network of AI-focused developers",
    icon: "🌟",
  },
];
---

<Layout>
  <!-- Hero Section -->
  <header
    class="relative bg-gradient-to-r from-gray-900 via-gray-800 to-gray-900 overflow-hidden"
  >
    <div
      class="absolute inset-0 bg-[url('/grid.svg')] bg-center [mask-image:linear-gradient(180deg,white,rgba(255,255,255,0))]"
    >
    </div>
    <div class="relative">
      <Navigation />

      <div class="flex justify-center mt-6 md:mt-8">
        <div class="inline-block animate-fade-in">
          <p
            class="text-xs md:text-sm bg-yellow-400/10 border border-yellow-400/20 rounded-full px-4 py-1.5 md:px-4 md:py-1.5 text-yellow-300 font-medium whitespace-nowrap"
          >
            🎯 Coming in Q2 2025 - Limited seats available!
          </p>
        </div>
      </div>

      <div class="container mx-auto px-6 py-16 md:py-24 text-center">
        <div class="animate-fade-in">
          <h1
            class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight text-white"
          >
            Building AI-Powered Applications
            <span
              class="block text-transparent bg-clip-text bg-gradient-to-r from-yellow-400 to-yellow-500 mt-2"
            >
              From Zero to AI Agent Mastery
            </span>
          </h1>
          <p
            class="text-lg md:text-xl mb-8 md:mb-12 max-w-2xl mx-auto text-gray-300"
          >
            Transform your development skills with our comprehensive workshop.
            Learn to build, deploy, and scale AI applications using JavaScript
            and modern tools.
          </p>
          <div
            class="flex flex-col sm:flex-row gap-4 justify-center items-center"
          >
            <a
              href="#register"
              class="w-full sm:w-auto bg-yellow-400 text-gray-900 px-6 md:px-8 py-3 md:py-4 rounded-lg text-base md:text-lg font-semibold hover:bg-yellow-300 transition-colors inline-flex items-center justify-center gap-2"
            >
              Join the Workshop <span class="text-xl">→</span>
            </a>
            <a
              href="#curriculum"
              class="w-full sm:w-auto text-white border border-gray-600 px-6 md:px-8 py-3 md:py-4 rounded-lg text-base md:text-lg hover:border-yellow-400 transition-colors text-center"
            >
              View Curriculum
            </a>
          </div>
        </div>
      </div>
    </div>
  </header>

  <!-- Features Section -->
  <section id="features" class="py-20 bg-gray-50 dark:bg-gray-900">
    <div class="container mx-auto px-6">
      <h2
        class="text-4xl font-bold text-center mb-16 text-gray-900 dark:text-white"
      >
        Workshop Highlights
      </h2>
      <div class="grid md:grid-cols-3 gap-12">
        {
          features.map((feature) => (
            <div class="bg-white dark:bg-gray-700 p-8 rounded-lg shadow-lg hover:shadow-xl transition-shadow">
              <div class="text-4xl mb-4">{feature.icon}</div>
              <h3 class="text-xl font-semibold mb-4 text-gray-900 dark:text-white">
                {feature.title}
              </h3>
              <p class="text-gray-600 dark:text-gray-300">
                {feature.description}
              </p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Curriculum Section -->
  <section id="curriculum" class="py-20 bg-white dark:bg-gray-900">
    <div class="container mx-auto px-6">
      <h2
        class="text-4xl font-bold text-center mb-16 text-gray-900 dark:text-white"
      >
        Workshop Curriculum
      </h2>
      <div class="grid gap-8">
        {
          sessions.map((session) => (
            <a
              href={session.href}
              class="group bg-gray-50 dark:bg-gray-800 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-xl p-6 transition-all"
            >
              <div class="flex flex-col md:flex-row gap-6">
                <div class="flex-none">
                  <span class="text-4xl font-bold text-yellow-600 dark:text-yellow-400">
                    {session.number}
                  </span>
                </div>
                <div class="flex-grow">
                  <h3 class="text-2xl font-semibold mb-2 text-gray-900 dark:text-white group-hover:text-yellow-600 dark:group-hover:text-yellow-400 transition-colors">
                    {session.title}
                  </h3>
                  <p class="text-gray-600 dark:text-gray-300 mb-4">
                    {session.description}
                  </p>
                  <div class="flex flex-wrap gap-2">
                    {session.topics.map((topic) => (
                      <span class="px-3 py-1 bg-white dark:bg-gray-700 rounded-full text-sm text-gray-700 dark:text-gray-300 border border-gray-200 dark:border-gray-600">
                        {topic}
                      </span>
                    ))}
                  </div>
                </div>
                <div class="flex-none self-center">
                  <span class="text-2xl text-gray-400 group-hover:text-yellow-600 dark:group-hover:text-yellow-400 transition-colors">
                    →
                  </span>
                </div>
              </div>
            </a>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Benefits Section -->
  <section
    id="benefits"
    class="py-20 bg-gray-900 text-white relative overflow-hidden"
  >
    <div
      class="absolute inset-0 bg-[url('/grid.svg')] bg-center [mask-image:linear-gradient(180deg,white,rgba(255,255,255,0))]"
    >
    </div>
    <div class="container mx-auto px-6 relative">
      <h2 class="text-4xl font-bold text-center mb-16">
        Why Join This Workshop?
      </h2>
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        {
          benefits.map((benefit) => (
            <div class="text-center p-6 rounded-lg bg-gray-800 bg-opacity-50 backdrop-blur-sm">
              <div class="text-4xl mb-4">{benefit.icon}</div>
              <h3 class="text-xl font-semibold text-yellow-400 mb-4">
                {benefit.title}
              </h3>
              <p class="text-gray-300">{benefit.description}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Email Capture Section -->
  <section id="register" class="py-20 bg-yellow-400">
    <div class="container mx-auto px-6">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-4xl font-bold text-gray-900 mb-8">
          Ready to Master AI Development?
        </h2>
        <p class="text-xl text-gray-800 mb-8">
          Join us for an intensive workshop that will transform you into a
          confident AI developer. Sign up your interest today!
        </p>
        <form class="max-w-md mx-auto flex flex-col gap-4" id="interest-form">
          <input
            type="email"
            name="email"
            placeholder="Enter your email"
            class="w-full px-4 py-3 rounded-lg border-2 border-gray-800 focus:outline-none focus:border-gray-900"
            required
          />
          <textarea
            name="message"
            placeholder="Share your thoughts, suggestions, or what you hope to learn from this workshop (optional)"
            rows="4"
            class="w-full px-4 py-3 rounded-lg border-2 border-gray-800 focus:outline-none focus:border-gray-900 resize-none"
          ></textarea>
          <button
            type="submit"
            class="bg-gray-900 text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-gray-800 transition-colors w-full flex items-center justify-center gap-2"
          >
            <span>Register Interest</span>
            <div id="submit-spinner" class="hidden">
              <svg
                class="animate-spin h-5 w-5 text-white"
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
              >
                <circle
                  class="opacity-25"
                  cx="12"
                  cy="12"
                  r="10"
                  stroke="currentColor"
                  stroke-width="4"></circle>
                <path
                  class="opacity-75"
                  fill="currentColor"
                  d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"
                ></path>
              </svg>
            </div>
          </button>
        </form>
        <div
          id="form-success"
          class="hidden mt-4 p-4 bg-green-100 text-green-700 rounded-lg"
        >
          Thanks for your interest! We'll keep you updated about the workshop.
        </div>
        <div
          id="form-error"
          class="hidden mt-4 p-4 bg-red-100 text-red-700 rounded-lg"
        >
          Sorry, there was an error submitting your form. Please try again.
        </div>
        <p class="mt-4 text-sm text-gray-700">
          By registering, you'll also get exclusive access to our JS Cebu
          community.
        </p>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-gray-900 text-gray-400 py-12">
    <div class="container mx-auto px-6">
      <div class="flex flex-col md:flex-row justify-between items-center gap-4">
        <div class="text-sm">
          © 2024 <a href="https://jscebu.org" class="hover:text-yellow-400"
            >JavaScript Cebu</a
          >. All rights reserved.
        </div>
        <div class="flex space-x-6">
          <a
            href="https://github.com/dorelljames/ai-jscebu"
            class="hover:text-yellow-400">GitHub</a
          >
        </div>
      </div>
    </div>
  </footer>
</Layout>

<script>
  const form = document.getElementById("interest-form") as HTMLFormElement;
  const submitSpinner = document.getElementById("submit-spinner");
  const formSuccess = document.getElementById("form-success");
  const formError = document.getElementById("form-error");

  if (form) {
    form.addEventListener("submit", async (e) => {
      e.preventDefault();

      // Show spinner
      submitSpinner?.classList.remove("hidden");
      formSuccess?.classList.add("hidden");
      formError?.classList.add("hidden");

      try {
        const formData = new FormData(form);
        const response = await fetch("/api/submit-interest", {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
          },
          body: JSON.stringify({
            email: formData.get("email"),
            message: formData.get("message"),
          }),
        });

        if (!response.ok) {
          throw new Error("Failed to submit form");
        }

        // Show success message
        form.reset();
        formSuccess?.classList.remove("hidden");
      } catch (error) {
        console.error("Error:", error);
        formError?.classList.remove("hidden");
      } finally {
        submitSpinner?.classList.add("hidden");
      }
    });
  }
</script>

<style>
  .animate-fade-in {
    animation: fadeIn 1s ease-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
